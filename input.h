#include <stdio.h>
int input() {
	puts("I honestly have no idea how I am going to handle the input (since i am no longer using fgets) so wait until I know how to handle input I guess.");
	return 0;
}
